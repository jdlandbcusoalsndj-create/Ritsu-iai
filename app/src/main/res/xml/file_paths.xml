<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio de archivos internos -->
    <files-path name="internal_files" path="." />
    
    <!-- Directorio de caché -->
    <cache-path name="cache_files" path="." />
    
    <!-- Directorio de archivos externos -->
    <external-files-path name="external_files" path="." />
    
    <!-- Directorio de caché externo -->
    <external-cache-path name="external_cache" path="." />
    
    <!-- Directorio de modelos de IA -->
    <files-path name="ai_models" path="ai_models/" />
    
    <!-- Directorio de grabaciones de llamadas -->
    <files-path name="call_recordings" path="call_recordings/" />
    
    <!-- Directorio de datos de voz -->
    <files-path name="speech_data" path="speech_data/" />
    
    <!-- Directorio de imágenes -->
    <files-path name="images" path="images/" />
    
    <!-- Directorio de documentos -->
    <files-path name="documents" path="documents/" />
</paths>